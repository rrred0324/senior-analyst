"""CLI entry points for senior_analyst: doctor, setup-keys.

Run as: ./venv/bin/python -m cli doctor
        ./venv/bin/python -m cli setup-keys

After install, also reachable as: senior_analyst-doctor (see setup.sh entry).
"""
